[package]
name = "calendry-solver-proto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Generated code only. No hand-written logic lives here, and it must never
# depend on calendry-solver-core: the conversion between the wire model and the
# dense search model is the service crate's job.
[dependencies]
tonic.workspace = true
tonic-prost.workspace = true
prost.workspace = true

[build-dependencies]
tonic-prost-build.workspace = true
